# CORRECCIONES_LOG

**Plan:** [`docs/PLAN_CORRECCIONES.md`](./PLAN_CORRECCIONES.md)
**Inicio de sesión:** 2026-07-10
**HEAD de partida:** `de3527c` (rama `beta`, ahead de `origin/beta` por 10 commits)
**D4 (descenso de contenido en prod):** confirmada por Jon como limpieza deliberada — no bloquea el arranque del plan.

## Línea base (C0.1)

| Puerta | Resultado |
|---|---|
| lint | 0 errores |
| typecheck | 0 errores |
| test | 196/196 (14 ficheros) |
| build | OK — `dist/assets/index-BpuvFw24.js` 284.20 kB (coincide con línea base A7) |

## Registro de tareas

| Tarea | Commit | Puertas | Incidencias | Verificación MANUAL pendiente |
|---|---|---|---|---|
| C0.1 | (sin commit, verificación) | lint 0 / typecheck 0 / test 196/196 / build OK | ninguna | — |
| C0.2 | `8bbbd7d` | lint 0 / typecheck 0 / test 196/196 / build OK | ninguna | — |
| C1.1 | `ba113d0` | lint 0 / typecheck 0 / test 199/199 / build OK (284.23 kB) | test rojo confirmado antes del fix (3 tests fallaban por `serializeIntervals` inexistente); ninguna otra incidencia | — |
| C1.2 | `57d3b8c` | lint 0 / typecheck 0 / test 199/199 / build OK (284.24 kB) | ninguna | **[MANUAL]** Jon: entrar, salir, recargar → debe aparecer login (no datos del usuario anterior); `localStorage` sin token `sb-*` |
| C1.3 | `6141667` | lint 0 / typecheck 0 / test 199/199 / build OK (284.31 kB) | ninguna; grep confirma que la rama 200 conserva "Correo enviado" | — |
| C1.6 | `f5a187d` | lint 0 / typecheck 0 / test 199/199 / build OK | `grep "components/modals" src/App.tsx` → 0 (cumple). `modals.tsx` ahora es su propio chunk async (`modals-*.js`, 28.66 kB) cargado solo por teacher/QuestionManagerView (ya lazy). **Bundle inicial: 284.31 kB → 255.92 kB, −28.39 kB.** No alcanza el objetivo ≥40 kB del informe (A7-04 estimaba "rendered" 54,1 kB, pero buena parte de eso son primitivas (`Overline`/`CtaButton`/`GhostButton`/`FieldLabel`) que ya vivían en el chunk inicial vía otros importadores — el ahorro real es solo el código exclusivo de `modals.tsx`. Cambio correcto y con el comportamiento intacto; no se fuerza extracción adicional fuera del alcance de la tarea. | — |
| C1.7 | `68ad7bb` | lint 0 / typecheck 0 / test 199/199 / build OK | **Incidencia relevante:** el gate `import.meta.env.DEV ? … : null` (incluso puesto en la MISMA expresión que la referencia, tal como sugiere el plan) NO basta para que Rollup pode `localSeed.ts` — verificado empíricamente con `git stash`/A/B: con ese gate el literal único `local-grados-cifrado` seguía en `index-*.js`, byte a byte idéntico al build sin el fix. Causa: Rollup mantiene un export "vivo" si CUALQUIER referencia alcanzable existe en el grafo, sin evaluar si la rama es constante-falsa; solo el minificador plegaría el ternario, pero para entonces Rollup ya decidió incluir el módulo. Se aplicó el fallback que el propio plan prevé: `import()` dinámico (en `App.tsx` para el usuario inicial + selector, y en `useAppData.ts` para exercises/users/results/courses/units/groups), con estado inicial vacío y población async en un `useEffect`. Verificado: `grep -r "local-grados-cifrado\|local-profe\|Lucía Arrieta" dist/assets/` → 0 coincidencias en NINGÚN asset (no solo index.js); de hecho no se genera ni siquiera un chunk `localSeed-*.js` — Rollup elimina el `import()` por completo, no solo lo separa. `index-*.js`: 255.92 kB → 253.07 kB. Verificación opcional en navegador (`npm run dev` + `?local`, puerto 5174 vía `cards`): confirmado con capturas — el profesor local carga con los ejercicios de la semilla (Coral BWV 227, Bourrée, Escucha global) sin errores de consola; el único cambio de comportamiento es un tick asíncrono adicional para poblar el estado (antes síncrono), irrelevante para una herramienta solo-dev. | **[MANUAL opcional]** Jon: `npm run dev` + `?local` y `?local=alumno` — confirmar que el selector de usuario y el resto de la app local siguen funcionando igual que antes (ya verificado por Claude Code, pendiente de tu ojo). |
| C1.8 | `3f369b9` | lint 0 / typecheck 0 / test 199/199 / build OK / `madge --circular` → 0 ciclos | El harness `src/preview-menu.tsx` (sin trackear, §1.9) importaba `KebabMenu` de `courses.jsx`; se actualizó su import localmente para no romper el harness, pero se dejó fuera de git (`git restore --staged`) tal como manda la regla — no se añade ni se borra. | — |
| C1.9 | `11190ed` | lint 0 / typecheck 0 / test 199/199 / build OK | ninguna | — |

**Fase C1 cerrada** (salvo C1.4 y C1.5, `[BLOQUEADA: D2/D3]`, no ejecutadas).

| C2.1 | `0f9ab09` | lint 0 / typecheck 0 / test 199/199 / build OK | ninguna | — |
| C2.2 | `1196b81` | lint 0 / typecheck 0 / test 199/199 / build OK | ninguna | — |
| C2.3 | `c25297a` | lint 0 / typecheck 0 / test 199/199 / build OK | Se mantuvo la letra de la opción visible en el círculo (no se sustituyó por ✓) y se añadió un badge ✓ superpuesto, para no perder la identidad A/B/C — más fiel a "además del relleno" que reemplazar el contenido. | — |
| C2.4 | `f3d7f28` | lint 0 / typecheck 0 / test 199/199 / build OK | `CircleButton`/`BarIconButton` ya reenviaban `title` como `aria-label` — solo hacía falta en botones `<button>` planos (icon-only) y en el play/pausa de Schema/QuestionManager sin `title`. La cita de línea 1777 del informe ya no aplica: ese botón ya tiene texto visible "Eliminar" (no es solo-icono); no se tocó (R11). | — |
| C2.5 | `9e819c9` | lint 0 / typecheck 0 / test 199/199 / build OK | ninguna | — |
| C2.6 | `1ba1789` | lint 0 / typecheck 0 / test 199/199 / build OK | `SchemaCorrection.tsx:141` no tenía literalmente `fontSize:0` (el informe lo citaba así) — usaba fontSize fijo 11 + `overflow:hidden`, con el mismo riesgo de invisibilidad total en bloques muy estrechos por una vía distinta (recorte CSS en vez de fontSize:0). Se aplicó el mismo criterio conservador (inicial visible, overflow visible bajo el umbral) por coherencia con el resto de vistas. En `session.tsx` (canvas, WaveformDisplay) no es viable un `title` por-región nativo sin overlay DOM adicional (fuera de alcance, no se tocó el mecanismo rAF, NO TOCAR §1.1); se garantizó igualmente el carácter mínimo dibujado. | **[MANUAL]** Jon valida visualmente con un ejercicio real de marcas/bloques densos (interactivo con cifrado, esquema con niveles estrechos); si el resultado molesta, iterar con su criterio. |
| C2.8 | `f8c2f74` | lint 0 / typecheck 0 / test 199/199 / build OK | En `SchemaCorrection.tsx` los tres bloques (niveles 3/4/resto) solo llevan `rowButtonProps` cuando `blockClickable` (si no, permanecen inertes, `pointerEvents:none`, correcto no darles foco). En `QuestionMinimap.tsx` solo en el modo "lectura" (gestor sin `onSeek`); el modo `marker` (sesión con `onSeek`) sigue inerte a propósito (el seek lo recoge la barra de fondo). | — |
| C2.9 | `0d5717e` | lint 0 / typecheck 0 / test 199/199 / build OK | `FieldLabel`/`TextInput`/`CredentialInput` ahora aceptan `htmlFor`/`id`; aplicado en SetupView, LoginView, ForgotPinView, ResetPinView y en `FragmentRangeSelector` (session.tsx, Inicio/Fin) con `useId()` para ids únicos por instancia (el componente puede montarse varias veces). Los formularios del editor (PasoAudios/PasoClaves/modals de preguntas) usan `<label style={S.label}>` sin `FieldLabel` y no se tocaron — alcance acotado, el barrido completo queda para PLAN_UNIFICACION (según el propio informe). | — |
| C2.10 | `a31c49d` | lint 0 / typecheck 0 / test 199/199 / build OK | Creada `SCHEMA_HND_HIT_W=40` (la `SCHEMA_HND_W` que citaba el comentario de `lib/schema.ts:28` no existía, confirmado — R11) y usada en `EDGE_PX` de `ExerciseView.tsx` (antes 14px de tolerancia, ahora 20px = 40px totales). En `session.tsx` los handles de fragmento pasan a una zona de arrastre invisible de 40px con la barra visible (12px) centrada dentro, sin cambiar el dibujo. Nueva clase global `.fa-hit40` (theme/fonts.ts, técnica `::before`) aplicada al botón Grabar/Regrabar de `PasoClaves.tsx` y a la × de `CategoryEditorModal` (modals.tsx:106). **No aplicada** a la × de opciones de test (modals.tsx antes L876, fila con solo 6px de separación): el hitbox de 40px se solaparía con la fila contigua y aumentaría el riesgo de borrar la opción equivocada — se dejó sin tocar (R5) y queda anotado aquí para que Jon decida si separar más las filas antes de aplicarlo. En `RepeatBand.tsx` las tres asas (inicio/unión/fin) pasan de 10-12px a 40px de hitbox con la barra visible intacta; la asa de unión sube a `zIndex:11` para seguir siendo agarrable cuando los tres hitboxes se solapan en repeticiones cortas. La cita de `SchemaExerciseView.tsx:1699` ya no correspondía a un asa de repetición (ahora es la barra de scroll del zoom) — el fix real se localizó por contenido en `RepeatBand.tsx` (R11). | **[MANUAL]** Jon prueba en móvil/táctil: agarrar las tres asas de una repetición corta (¿se puede seleccionar cada una de forma fiable?) y los handles de fragmento en session.tsx. |
| C2.11 | `fcaa1f4` | lint 0 / typecheck 0 / test 199/199 / build OK | ninguna | — |
| C2.12 | `5f7b741` | lint 0 / typecheck 0 / test 199/199 / build OK | `aria-valuenow`/`aria-valuetext` se actualizan dentro del mismo bucle rAF (vía `setAttribute`, sin re-render de React) para no romper el patrón de rendimiento de NO TOCAR §1.1. Sirve a las 4 vistas que reusan `AudioScrubber`. | — |
| C2.13 | `398c9c4` | lint 0 / typecheck 0 / test 199/199 / build OK | **"Curso sin unidades" ya existía** (courses.tsx:314 y :515, mensaje "Este curso no tiene unidades todavía.") — no era un vacío que faltara (R11, cita del informe desactualizada); no se tocó. Para "Guardando…": el guardado local es síncrono (dbUpsertExercise persiste en 2º plano, fuera del alcance de este componente) — se optó por un estado transitorio local (700ms tras el clic) en vez de intentar hilar un `isSaving` real desde useAppData, desproporcionado para esta tarea. | — |

**Fase C2 cerrada.**

| C3.1 | `0df649f` | lint 0 / typecheck 0 / test 199/199 / build OK | Banner arriba (ámbar) para distinguirlo visualmente del toast rojo de guardado (abajo); no se autooculta (persiste mientras `loadError` sea no-nulo, a diferencia de `saveError` que sí se autooculta a los 9s — intencional, ver comentario en el código). | **[MANUAL]** Jon: cortar red / URL de Supabase inválida en dev → banner visible arriba, "Reintentar" recarga los datos. |
| C3.2 | `938d548` | lint 0 / typecheck 0 / test 199/199 / build OK | `saveSessionUser`/`readSessionUser`/`clearSessionUser` en `authClient.ts` (perfil mínimo `{id,role}`, nunca fuente de verdad — siempre se verifica contra `loaded.users` recién cargados con RLS antes de restaurar sesión). `logout()` ya limpia el perfil rehidratable, así que C1.2 no necesitó cambios adicionales. `onAuthStateChange` vive en App.tsx (no en useAppData) porque necesita `navigate`/`setUser`, que son de App. | **[MANUAL]** Jon: login → recargar → sigue dentro; salir → recargar → login; segunda pestaña + salir en la primera → la segunda reacciona al SIGNED_OUT. |
| C3.3 | `56d72b1` | lint 0 / typecheck 0 / test 199/199 / build OK | Reintentos con backoff corto (400/800ms, 2 reintentos) dentro de `checkHasAdmin`; tras agotarlos, se integra con `loadError`/el banner de C3.1 en vez de crear un banner nuevo — "Reintentar" ya vuelve a intentar tanto la carga como `has_admin`. `serverHasAdmin` nunca se fuerza a `false` por un fallo de red (se queda en `null`), así que `noAdmin` (App.tsx:361) sigue sin mostrar SetupView por defecto. | — |
| C3.4 | `a9ce1f0` | lint 0 / typecheck 0 / test 203/203 / build OK | Test rojo confirmado antes del fix (2 tests fallaban con `TypeError`). Único consumidor real de `btnOf` en todo el repo: `InteractiveCorrection.tsx:80`; se protegió con un fallback `{id, color: C.muted}` ya que `b.color` se leía sin guard. | — |
| C3.5 | `124acb9` | lint 0 / typecheck 0 / test 209/209 / build OK | Test rojo confirmado antes del fix (5 tests fallaban con `flattenSinglePart is not a function`; el test que demuestra el bug SIN llamar a la función ya pasaba, confirmando que captura el comportamiento roto real). Punto de integración elegido: `handleSave`, no `removePart` — `removePart` solo muta el estado local del editor (`parts`), la persistencia real solo ocurre en `handleSave`/`onUpdate`, único punto de escritura (verificado, no hay otra vía). `patch.parts = undefined` explícito es necesario: `updateExercise` hace un merge superficial (`{...current, ...patch}`) — sin limpiar `parts`, el array viejo de 2 elementos sobreviviría con longitud >1 y `partsOf` lo devolvería tal cual, sin aplanar nada. Se exportó `PART_FIELDS` de `domain.ts` (antes privado) para reutilizar la misma lista en el editor sin duplicarla. | — |
| C3.6 | `9714b56` | lint 0 / typecheck 0 / test 209/209 / build OK | Mismo mensaje y patrón (`urlReqRef` para cancelar detecciones obsoletas) que ya usaba el modal del almacén (`modals.tsx`); `EditorApi` es un tipo inferido (`ReturnType<typeof useExerciseEditor>`), no hizo falta tocar ninguna interfaz a mano. | — |
| C3.7 | — | — | `[BLOQUEADA: D8]` — no ejecutada. | — |
| C3.8 | `0d69851` | lint 0 / typecheck 0 / test 214/214 / build OK | El bug real: al pegar una URL en una parte (multiparte), `PasoAudios.tsx` solo fijaba `audioUrl`/`audioName` — nunca decodificaba para fijar `duration` (a diferencia de `handleUrlInput` de una sola parte, y a diferencia de elegir del almacén, donde la duración ya viene calculada). Se añadió `handlePartUrlInput` (mismo patrón de decodificación + `partUrlReqRef` por parte para cancelar peticiones obsoletas) y `partsAudioReady` en `canSave` (toda parte CON audio debe tener duración > 0). No se creó suite de tests para `useExerciseEditor.ts` (no existía ninguna previa; el pedido explícito de tests del informe era sobre `calcScore`, ya cubierto). | — |

**Fase C3 cerrada** (salvo C3.7, `[BLOQUEADA: D8]`, no ejecutada).

| C4.1 | `b8a7ed4` | lint 0 / typecheck 0 / test 214/214 / build OK | El default literal que sugiere el informe (`= uid`) cambiaría el prefijo de "sb" a "id" para los 3 llamantes reales (SchemaExerciseView.tsx×2, useSchemaEditor.ts — `ExerciseView.tsx` que citaba el informe no es llamante real, R11). Se usó `= () => uid("sb")` para reproducir el comportamiento EXACTO actual. No se tocó `uid()` en `ids.ts` (la parametrización `now?/rand?` era opcional "(+)"; C4.2 puede inyectar determinismo vía `makeId` sin ella). | — |
| C4.2 | `8c073cf` | lint 0 / typecheck 0 / test 239/239 (15 ficheros) / build OK | Cobertura de `repeats.ts` medida con `@vitest/coverage-v8` (`npm install --no-save`, artefactos borrados, `git status` limpio en package*.json — R10): **100% stmts / 100% funcs / 100% líneas / 92.4% ramas**, supera ampliamente el ≥90% exigido. 25 tests: los 6 casos de `buildRepeatSegments`, hueco/sin-hueco/orden en `buildCompleteViewSegments`, 5 casos de `getSegBounds` (no pedido explícitamente por el informe pero necesario para la cobertura), 8 casos de `syncSecondPassBlocks` (incluida la nueva rama del `makeId` por defecto de C4.1) y 5 de `rulerTicksForSeg`. Todos los valores esperados se calcularon a mano trazando el algoritmo y se verificaron contra el comportamiento real (ninguno sorprendió — no hubo que documentar un caso "raro"). | — |
| C4.3a | `406fd13` | lint 0 / typecheck 0 / test 239/239 / build OK | `SchemaPalettePicker` extraído con estado propio (paletteOpen/paletteRef/click-outside) — el padre solo pasa `schemaPalette`+`onChange`, nada más lo necesitaba (grep confirmado). Verificado en navegador (`?local`, previsualizar "Coral BWV 227"): abre mostrando las 5 paletas, seleccionar una mueve el ✓ y se refleja en el padre, clic fuera cierra el desplegable. El asistente `computer{screenshot}` falló repetidamente por timeout en esta sesión (sin relación con el cambio) — la verificación se hizo con `read_page`/`get_page_text`/`javascript_tool` en su lugar. | — |
| C4.3b | `3776434` | lint 0 / typecheck 0 / test 239/239 / build OK | `BlockDetailPanel` extraído (panel de bloque/repetición/vacío + área de texto nivel 4), ~15 props explícitas, sin tocar dragRef/trackSegRefs. **Incidencia corregida antes de commitear:** el primer borrador inventó un color hardcodeado (`#8B5CF6`) para la etiqueta del área de texto en vez de usar `SCHEMA_LEVELS[3].color` (el valor real del original) — detectado en autorevisión antes de las puertas, corregido importando `SCHEMA_LEVELS` en el nuevo fichero. Verificado en navegador: crear bloque por arrastre simulado (mousedown/mousemove/mouseup con coordenadas reales), el panel muestra etiqueta/duración/Renombrar/Eliminar; Renombrar activa correctamente el input inline en la pista (confirma que el puente de props con el resto del padre, aún no extraído, funciona); Eliminar quita el bloque y vuelve al vacío. | — |

